<UserControl x:Class="UserControlLibrary.Commons.PanelAdapter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserControlLibrary.Commons"
             mc:Ignorable="d" 
             >
    <Grid >
        <ItemsControl ItemsSource="{Binding PanelAdapterModels}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border  Margin="4" >
                        <StackPanel  HorizontalAlignment="Center" Width="80">
                            <TextBlock Text="{Binding ItemValue}" Foreground="#00E5FF" FontSize="25" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding ItemName}" Foreground="White" FontSize="12" Margin="0,4,0,5" HorizontalAlignment="Center"/>
                        
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
